# 零基础导读 · 南瓜工具箱

> 写给：学过 C/C++ 或 Java 基础、看过一点 Kotlin、第一次接触 Compose 的大二同学。
> 目标：让你能看懂这个项目里"和你的任务相关"的代码，并最终自己写出「晚间自动显示明日课程」。
> 配套：`TODO.md`（要做的清单）、`README.md`（项目简介）。

---

## 第 0 章 · 怎么用这份导读

### 0.1 先记住一句话
**你不需要看懂整个项目。** 这个项目有很多功能（成绩、考试、评教、扫码……），你只需要看懂其中一条线：**课表**，再缩小一点，是**「今日课程」页**。

### 0.2 推荐路线
按顺序读：第 1 章（Kotlin）→ 第 2 章（Compose）→ 第 5 章（逐行精读）。
第 3、4 章是补充，读到相关代码时再回来查即可。

### 0.3 最省时路线（每天只有 1 小时的话）
第 1 章（挑"空安全 / Lambda / 集合操作 / object"看）→ 第 2 章（重点"状态与重组"）→ 第 5.6 节（`TodayScreen.kt`）。

### 0.4 卡住了怎么办
把你看不懂的**那几行**复制发给 AI 助手，问："这几行逐行解释一下，我是 Java 基础。" 不要一次发整个文件。

### 0.5 读代码时的"三问"
每看完一段代码，试着回答：
1. 它的**输入**是什么？
2. 它的**输出**是什么？
3. 谁**调用**它？

答不上来，说明还没懂，别硬往下读。

---

## 第 1 章 · Kotlin 速通（对照 Java）

Kotlin 可以理解成"写起来更短的 Java"。下面每一条都先给 Java 对照。

### 1.1 变量：`val` 和 `var`

```kotlin
val a = 10      // 只读，相当于 Java 的 final int a = 10;
var b = 20      // 可修改
b = 30
```

- `val` = 不能重新赋值（类似 `final`）。
- `var` = 可以重新赋值。
- **类型不用写**，Kotlin 能自己推断出来（叫"类型推断"）。需要时也能显式写：`val a: Int = 10`。

> 注意：Kotlin 里没有基本类型和包装类型的区分，`Int` 就是 `int`，写起来统一。

### 1.2 函数：`fun`

```kotlin
// Java: int add(int a, int b) { return a + b; }
fun add(a: Int, b: Int): Int {
    return a + b
}

// 只有一行时，可以写成表达式（等价）
fun add2(a: Int, b: Int): Int = a + b

// 没有返回值用 Unit（可省略）
fun hello() {
    println("hi")
}
```

- 参数写法是 `名字: 类型`，和 Java 的 `类型 名字` 相反。
- 返回类型写在参数括号后面，用 `:` 连接。

**默认参数 / 命名参数**（Java 里要写重载，Kotlin 一个函数搞定）：

```kotlin
fun greet(name: String, greeting: String = "你好") = "$greeting, $name"

greet("小明")                  // 你好, 小明
greet("小明", greeting = "早上好") // 早上好, 小明
```

> 项目里 `NumberPicker(...)`、`Card(...)` 这种组件几乎都靠默认参数，不传就用默认值。

### 1.3 `data class`：专门装数据的类

```kotlin
// Java 要写一堆字段 + getter/setter + equals/hashCode/toString
data class Course(
    var name: String,
    var classroom: String
)
```

`data class` 会自动生成 `equals`、`hashCode`、`toString`、`copy`。项目里的 `Course`（`models/Course.kt`）就是一个 `data class`。

- **注意**：用 `data class` 声明属性时，Kotlin 默认就生成了 getter/setter，直接用 `course.name` 访问，不用 `getName()`。

### 1.4 `object`：单例

```kotlin
object AppConfig {
    var username = ""
}
// 使用：AppConfig.username = "zhang"
```

`object` 声明的类**全世界只有一个实例**，类似 Java 里手写的 `getInstance()` 单例。
项目的全局配置 `AppConfig`（`AppConfig.kt`）就是这么写的。

### 1.5 空安全（重点！本项目到处都是）

Java 里对象可能是 `null`，不检查就会 `NullPointerException`。Kotlin 把"可能为空"写进类型系统：

```kotlin
var s: String = "abc"    // 不能为 null
var t: String? = null    // 多了个 ?，表示"可能是 null"

t.length        // ❌ 编译不过，因为 t 可能为 null
t?.length       // ✅ 安全调用：t 为 null 时整体返回 null
t!!.length      // ⚠️ 强制断言：如果 t 是 null 就崩溃（尽量别用）
```

**Elvis 运算符 `?:`**：左边为空就用右边的值。

```kotlin
val len = t?.length ?: 0      // t 为空就用 0
val name = cache.name ?: "未知" // 取不到就用默认值
```

看到 `?.` 和 `?:` 别紧张，翻译成人话就是："如果不为空就取它，否则……"。

### 1.6 Lambda 与末尾 lambda（重点！）

Lambda 就是"一小段可以当参数传进去的函数"。

```kotlin
// Java: list.forEach(x -> System.out.println(x));
list.forEach { x -> println(x) }

// 如果 lambda 只有一个参数，可以省略不写，用 it 指代
list.forEach { println(it) }
```

**末尾 lambda**：如果函数最后一个参数是 lambda，可以把它写到括号外面（甚至省掉括号）。这是 Compose 里最常见的写法：

```kotlin
Button(onClick = { /* 点击时执行 */ }) {
    Text("按钮")   // 这个 { } 就是末尾 lambda，表示按钮里的内容
}
```

### 1.7 集合操作（课表代码全靠它）

```kotlin
val nums = listOf(3, 1, 2)

nums.filter { it > 1 }        // [3, 2]   筛选
nums.map { it * 10 }          // [30, 10, 20] 变形
nums.sortedBy { it }          // [1, 2, 3] 排序
nums.first()                  // 3
nums.getOrElse(5) { -1 }      // -1（下标越界就用默认值）

// 多个条件筛选
val courses = listOf(/* ... */)
courses.filter { it.dayOfWeek == 1 && it.duration == 2 }
```

> 看 `TodayScreen.kt:61` 那行 `.filter { ... }`，现在你应该能读懂了。

### 1.8 `when`：加强版 switch

```kotlin
when (mode) {
    1 -> println("夏秋")
    2 -> println("冬春")
    else -> println("自动")
}
```

- `else` 相当于 Java 的 `default`。
- 项目里 `AppConfig.kt:42` 用它根据季节选时间表。

### 1.9 扩展函数：给已有的类"加方法"

```kotlin
// 给 List<Course> 加一个方法
fun List<Course>.countByDay(day: Int): Int = filter { it.dayOfWeek == day }.size
```

调用时就像这个方法本来就有：`courses.countByDay(1)`。
项目里 `Extensions.kt` 的 `buildWeekCourses(courses)` 就是给 `List<Course>` 加的方法。

### 1.10 `by` 委托（Compose 状态会用到）

```kotlin
var title by remember { mutableStateOf("课程表") }
// 之后就能直接：
title = "第1周"      // 赋值
println(title)      // 读取
```

`by` 让"读取/赋值"自动转发到右边那个对象上。你只要知道：**`by mutableStateOf(...)` 的东西，直接当普通变量读写即可**。

### 1.11 字符串模板

```kotlin
val week = 3
val s = "第${week}周"      // 第3周
val s2 = "第${pagerState.currentPage + 1}周"  // 里面可以放表达式
```

等价于 Java 的字符串拼接，但更简洁。

### 1.12 本章自测
1. `val` 和 `var` 的区别？
2. `t?.length ?: 0` 是什么意思？
3. `list.filter { it.dayOfWeek == 1 }` 的结果是什么？

---

## 第 2 章 · Compose 速通

Compose 是 JetBrains 的 UI 框架（Android/桌面/iOS 通用）。它和传统 Android 写的 `XML 布局` 完全不同。

### 2.1 声明式 UI：用函数"画"界面

传统写法（命令式）：先找到按钮 `findViewById`，再设置文字、绑定点击。
Compose 写法（声明式）：**写一个函数，描述"界面长什么样"**。

```kotlin
@Composable
fun Hello() {
    Text("你好")
}
```

- `@Composable` 是一个标记，表示"这是一个 UI 函数"。
- 这种函数内部只能调用别的 `@Composable` 函数，不能随便写普通代码。

### 2.2 状态与重组（本项目最核心概念，务必看懂）

问题：界面要显示一个数字，点一下加 1，怎么让界面跟着变？

```kotlin
@Composable
fun Counter() {
    var count by remember { mutableStateOf(0) }   // 1. 声明状态

    Column {
        Text("当前：$count")
        Button(onClick = { count++ }) {           // 2. 改状态
            Text("加一")
        }
    }
}
```

逐行解释：
1. `remember { mutableStateOf(0) }`：创建一个"能被 Compose 观察的状态"，初始值 0。
   - `remember` = 记住这个值，别每次重画都重置。
   - `mutableStateOf` = 一个"有变化时会被感知"的盒子。
   - `by` = 让你直接写 `count`，不用写 `count.value`。
2. 点按钮改 `count`。

**关键机制叫"重组"（recomposition）**：当状态变了，Compose 会**自动重新执行**用到这个状态的 `@Composable` 函数，于是界面更新。
你不需要手动"刷新界面"，改状态就行。

> 为什么重要：你的功能里，"是否显示明天"就是一个状态。过了时间点后，改这个状态，界面就会自动切换。**但前提是得有人去改它**——这就是 `LaunchedEffect` 的活。

### 2.3 `LaunchedEffect`：进入页面后跑一段"副作用"

```kotlin
LaunchedEffect(Unit) {
    delay(1000)          // 等 1 秒
    println("1 秒后执行")
}
```

- `LaunchedEffect` 里的代码会在组件第一次显示时启动。
- 括号里的参数叫"钥匙"：钥匙变了，就重新启动这段代码。`Unit` 表示"只在第一次跑"。
- 里面可以 `delay`、发网络请求等（这些需要协程，见第 3 章）。

> 你的"定时刷新"就靠它：让它在后台每隔一段时间重新判断"现在该显示今天还是明天"。

### 2.4 布局三兄弟：`Column` / `Row` / `Box`

```kotlin
Column {        // 竖着排（垂直）
    Text("第一行")
    Text("第二行")
}

Row {           // 横着排（水平）
    Text("左")
    Text("右")
}

Box {           // 叠着放（像层叠）
    Box(Modifier.fillMaxSize().background(Color.Red))
    Text("我在最上层")
}
```

- `Modifier` = 给组件加样式/尺寸/边距的"修饰符链"。

```kotlin
Text(
    text = "你好",
    modifier = Modifier
        .fillMaxWidth()          // 占满宽度
        .padding(16.dp)          // 四周留白 16dp
)
```

- `.dp` 是尺寸单位（类似 Android 的 dp）。
- `Modifier` 是**链式调用**，从左到右依次生效。

### 2.5 常见组件

| 组件 | 作用 |
|---|---|
| `Text` | 显示文字 |
| `Button` / `IconButton` | 按钮 |
| `Card` | 卡片容器 |
| `Switch` | 开关 |
| `Column`/`Row`/`Box` | 布局 |
| `LazyColumn` | 可滚动的长列表 |
| `Scaffold` | 页面骨架（顶栏/底栏） |

### 2.6 `@Composable` 的参数与状态提升

好的 Compose 习惯：**状态放在上层，组件只负责显示 + 回调**。

```kotlin
@Composable
fun MySwitch(checked: Boolean, onChange: (Boolean) -> Unit) {
    Switch(checked = checked, onCheckedChange = onChange)
}
```

- `checked`：当前状态（由外面传进来）。
- `onChange`：用户操作后的回调（让外面去改状态）。

> 项目里 `AppConfig.tomorrowScheduleEnabled` 就是"全局状态"，`Switch` 只负责显示和回调。

### 2.7 本章自测
1. `remember { mutableStateOf(0) }` 是干什么的？
2. 什么是"重组"？为什么改状态界面就会更新？
3. `LaunchedEffect` 一般用来做什么？

---

## 第 3 章 · 协程与时间

### 3.1 协程：能"暂停"的函数

```kotlin
suspend fun loadData() {      // suspend = 可以暂停
    delay(1000)               // 暂停 1 秒，但不卡住界面
    println("加载完成")
}

// 在别的地方启动它
LaunchedEffect(Unit) {
    loadData()
}
```

- `suspend` 标记的函数叫"挂起函数"，只能在协程里调用。
- `delay(1000)` = 等 1000 毫秒，等价 Java 的 `Thread.sleep`，但**不会卡住界面**。
- `launch { }` 用来启动一段协程（比如点击事件里）。

你只需要**会读**，不用会写复杂的协程。

### 3.2 时间工具 `kotlinx.datetime`

```kotlin
import kotlin.time.Clock
import kotlinx.datetime.TimeZone
import kotlinx.datetime.toLocalDateTime
import kotlinx.datetime.LocalDate
import kotlinx.datetime.DateTimeUnit
import kotlinx.datetime.plus

// 拿到"现在"，并转换成本地时间
val now = Clock.System.now()
    .toLocalDateTime(TimeZone.currentSystemDefault())

val today: LocalDate = now.date     // 日期
val hour = now.hour                 // 时
val minute = now.minute             // 分

// 日期加一天
val tomorrow = today.plus(1, DateTimeUnit.DAY)

// 今天星期几？dayOfWeek 的顺序是：周一=0, 周二=1, ... 周日=6
val iso = today.dayOfWeek.ordinal   // 0..6
```

> **重要的坑**：本项目里课程用的 `dayOfWeek` 是 `0=周日, 1=周一, …, 6=周六`。
> 而 `today.dayOfWeek.ordinal` 是 `0=周一, …, 6=周日`。
> 所以项目里用一个公式来转换（见 5.6 节）：
> `课程day = (today.dayOfWeek.ordinal + 1) % 7`

### 3.3 本章自测
1. `suspend` 是什么意思？
2. `Clock.System.now().toLocalDateTime(...)` 得到什么？
3. 日期怎么加一天？

---

## 第 4 章 · 本项目的架构约定

### 4.1 KMP（Kotlin Multiplatform）与 `expect` / `actual`

这个项目一份代码同时给 Android 和 iOS 用。大部分代码放在 `shared/src/commonMain`（通用），少部分平台相关的放在 `androidMain` / `iosMain`。

当"每个平台实现方式不同"时，用 `expect` / `actual`：

```kotlin
// commonMain：声明"我要一个工具"
expect fun getPlatform(): Platform

// androidMain：安卓的实现
actual fun getPlatform() = AndroidPlatform()

// iosMain：iOS 的实现
actual fun getPlatform() = IosPlatform()
```

**你只需要知道这个概念**，你的功能基本用不到。

### 4.2 `AppConfig`：全局配置中心

位置：`AppConfig.kt`。它是一个 `object`（单例），保存所有用户设置，比如：
- `timeSeason`（夏秋/冬春时间表）
- `username` / `password`
- `cellHeight`（课表格子高度）

**它的固定套路**（你新增设置项就照抄）：

```kotlin
object AppConfig {
    // 1) 定义字段（mutableStateOf 让 Compose 能感知变化）
    var timeSeason by mutableIntStateOf(0)

    // 2) 定义一个 key
    object KEY {
        const val TIME_SEASON = "time_season"
    }

    // 3) load()：启动时从存储读出来
    fun load() {
        timeSeason = kvault.getInt(KEY.TIME_SEASON) ?: 1
    }

    // 4) save()：改完写回存储
    fun save() {
        kvault.putInt(KEY.TIME_SEASON, timeSeason)
    }
}
```

- `kvault` 是加密的键值存储（安卓用加密的 SharedPreferences，iOS 用 Keychain）。
- `?: 1` 表示"读不到就用默认值 1"。

### 4.3 ViewModel 和 CompositionLocal

- `AppViewModel`（`viewmodel/AppViewModel.kt`）保存"跨页面共享的数据"，最主要的是 `courseList`（所有课程）和 `currentWeek`（当前周）。
- 页面怎么拿到它？通过 `LocalAppViewModel.current`：

```kotlin
@Composable
fun TodayScreen(
    viewModel: AppViewModel = LocalAppViewModel.current   // 默认值就是取全局那个
) {
    val courses = viewModel.courseList
}
```

- `LocalXXX` 是 Compose 的"全局注入"机制（叫 CompositionLocal）。在 `App.kt` 里注入，在任意页面里取。
- 你**会读** `LocalAppViewModel.current` 即可，不用会配。

### 4.4 存储与序列化

- `FileStoreUtils`：往文件里读写字符串（用来缓存课表）。
- `JsonUtil` + `@Serializable`：把对象和 JSON 互相转换。`data class` 头上加 `@Serializable` 就能存成 JSON。
- 你暂时用不到，知道"数据存在哪"就行。

### 4.5 本章自测
1. `expect` / `actual` 是干什么的？
2. `AppConfig` 新增一个设置项要改哪几处？（提示：字段、KEY、load、save）
3. `LocalAppViewModel.current` 是什么？

---

## 第 5 章 · 逐行精读

> 这一章是重点。建议打开 IDE，一边看这个文件，一边看源码对照。

### 5.1 iOS 入口：`shared/src/iosMain/.../MainViewController.kt`

```kotlin
fun MainViewController() = ComposeUIViewController {
    App(
        viewModel = viewModel(factory = AppViewModel.Factory)
    )
}
```

逐行：
- `ComposeUIViewController { ... }`：把 Compose 界面包装成 iOS 能用的 ViewController。
- 里面放的 `App(...)` 就是整个应用。

**结论**：iOS 和安卓跑的是同一套 `App()`，也就是 `commonMain` 里的代码。你改 `commonMain`，两头都变。

### 5.2 App 外壳：`App.kt`

关键片段（约 79~117 行）：

```kotlin
@Composable
fun App(viewModel: AppViewModel = viewModel(factory = AppViewModel.Factory)) {
    val navigator = remember(viewModel.backStack) { Navigator(viewModel.backStack) }
    val coroutineScope = rememberCoroutineScope()
    AppConfig.load()                       // ← 启动时读取配置

    // ★ 当 startDate 变化时，重新计算"当前是第几周"
    LaunchedEffect(AppConfig.startDate) {
        AppConfig.startDate?.let {
            val weekCalculator = WeekCalculator(AppConfig.startDate!!, 1)
            viewModel.currentWeek = weekCalculator.getWeekNumber(AppConfig.localDate).toInt()
        }
        // ...保存小组件数据、刷新...
    }

    // 读取本地缓存的课表
    coroutineScope.launch {
        val courseStr = FileStoreUtils.readString(FileName.SCHEDULE)
        courseStr?.let {
            val cache = JsonUtil.parseJson(it, ScheduleCache.serializer())
            viewModel.courseList.clear()
            viewModel.courseList.addAll(cache.courses)
        }
    }
    // ...登录、注入导航...
}
```

要记住的两件事：
1. `viewModel.currentWeek` 只在 `startDate` 变化时算一次。**它不会在半夜自动变成新的一周**——这是你功能里的坑之一。
2. `AppConfig.localDate`（`AppConfig.kt:21`）是 App 启动那一刻算好的，跨天后就过期了。**所以判断"今天/明天"必须用实时的 `Clock.System.now()`**。

### 5.3 配置：`AppConfig.kt`

对照 4.2 的套路读：
- `:32-46` 一堆设置字段 + `timeList` 的选择。
- `:64-86` 所有 `KEY` 常量。
- `:89-124` `load()` 从存储读。
- `:126-149` `save()` 写回存储。

你要加的三个字段（见 `TODO.md`），就是完全照抄 `timeSeason` 的写法。

### 5.4 周次计算：`WeekCalculator.kt`

核心方法：

```kotlin
fun getWeekNumber(date: LocalDate): Long {
    val targetSunday = getSundayOfWeek(date)          // 先找到这一天所在周的周日
    val dayDiff = baseSunday.until(targetSunday, DateTimeUnit.DAY)  // 和基准周差几天
    return dayDiff / 7 + baseWeekNum                  // 差几天 ÷ 7 = 差几周
}
```

- 规则：**周日算一周的第一天**。基准日 `2026-03-01`（周日）是第 1 周。
- 给它一个日期，返回它属于第几周（可能是 0 或负数，代表假期）。

> 你的功能里，算"明天是第几周"必须用它，而且要用**明天的日期**去算（因为周六的明天=周日=下一周）。

### 5.5 构建每周课表：`Extensions.kt` 的 `buildWeekCourses`

```kotlin
fun buildWeekCourses(courses: List<Course>): List<List<Course>> {
    val courseListByWeek = mutableListOf(mutableListOf<Course>())
    courses.forEach { course ->
        course.weeks.forEach { week ->
            // 如果周数超过列表长度，就补空列表
            if (week[1] > courseListByWeek.size) {
                for (i in courseListByWeek.size..< week[1]) {
                    courseListByWeek.add(mutableListOf())
                }
            }
            // 把这门课加进它出现的每一周
            for (i in week[0]-1..< week[1]) {
                courseListByWeek[i].add(course)
            }
        }
    }
    return courseListByWeek
}
```

- 输入：所有课程的列表。
- 输出：`List<List<Course>>`，**下标 0 = 第 1 周，下标 1 = 第 2 周**，依此类推。
- 所以"取第 N 周的课"就是 `list.getOrElse(N - 1) { emptyList() }`。

### 5.6 主战场：`TodayScreen.kt`（全篇逐行）

这是你现在最该看懂的文件。以下按行号讲解。

```kotlin
@Composable
fun TodayScreen(
    modifier: Modifier = Modifier,
    viewModel: AppViewModel = LocalAppViewModel.current   // 默认取全局 ViewModel
) {
    // 判断是否登录（用户名和密码都非空）
    val loggedIn = AppConfig.username.isNotEmpty() && AppConfig.password.isNotEmpty()

    // 把所有课程按周摊开：courses[0] 是第 1 周的课
    val courses = buildWeekCourses(viewModel.courseList)
```

```kotlin
    // 拿到"当前这一周"的全部课程。下标要 -1，因为列表从 0 开始。
    // getOrElse(...) { emptyList() }：万一越界，就用空列表，防止崩溃。
    val weekCourses = courses.getOrElse(viewModel.currentWeek - 1) { emptyList() }
```

```kotlin
    // 拿当前日期
    val localDate = Clock.System.now()
        .toLocalDateTime(TimeZone.currentSystemDefault()).date

    // ★ 今天有课的日子，筛出"今天"的课
    val todayCourses = weekCourses.filter { course ->
        // course.dayOfWeek: 0=周日,1=周一,...,6=周六
        // localDate.dayOfWeek.ordinal: 0=周一,...,6=周日
        // (ordinal + 1) % 7 把后者换算成前者
        course.dayOfWeek == (localDate.dayOfWeek.ordinal + 1) % 7
    }
```

> **这两行就是你要改的核心。** 现在它永远用"今天"的日期和 `viewModel.currentWeek`。
> 你要做的是：根据当前时间和设置，决定用"今天"还是"明天"的日期，并把对应周的课程筛出来。

```kotlin
    Scaffold(
        topBar = {
            SmallTopAppBar(
                title = "今日课程",       // ← 标题，将来要变成"今日课程"/"明日课程"
                // ...
            )
        }
    ) { paddingValues ->
        LazyColumn(...) {              // 可滚动列表
            items(todayCourses.size) { index ->
                val course = todayCourses[index]
                // 去掉教室名里的校区前缀
                val classroom = course.classroom
                    .replace("【红湘校区】", "")
                    .replace("【雨母校区】", "")

                // 把"第几节"换算成具体时间（timeList[几] -> "08:00"-"08:45"）
                val startIndex = (course.lessonOfDay - 1).coerceIn(0, AppConfig.timeList.size - 1)
                val endIndex = (startIndex + course.duration - 1).coerceIn(0, AppConfig.timeList.size - 1)

                Card(...) {
                    BasicComponent(
                        title = course.name,
                        summary = "${AppConfig.timeList[startIndex].start}-" +
                                "${AppConfig.timeList[endIndex].end} " + course.teacher,
                        endActions = {
                            Text(text = classroom)   // 右侧显示教室
                        },
                    )
                }
            }

            // 没有课或没登录时的提示
            if (!loggedIn || todayCourses.isEmpty()) {
                item {
                    Text(text = if (loggedIn) "暂无课程" else "请登录使用")
                }
            }
        }
    }
}
```

**这段代码的"数据流"总结**：
```
viewModel.courseList
   │ buildWeekCourses()  → 按周分组
   ▼
courses[周-1] = 那一周的所有课
   │ filter { dayOfWeek == 今天 }
   ▼
todayCourses = 今天要显示的课
   │
   ▼
LazyColumn 逐条画成卡片
```

**你要改成的样子**（思路，不是完整代码）：
```
当前时间 >= 设置时间 ？
   ├─ 否 → 用今天日期、当前周 → 标题"今日课程"
   └─ 是 → 用明天日期、明天所在周 → 标题"明日课程"
```

注意几个坑（`TODO.md` 第七节也写了）：
- **明天可能跨周**：不能直接用 `viewModel.currentWeek`，要用 `WeekCalculator(getWeekNumber(明天))` 重新算。
- **假期**：算出来的周数 `<= 0` 或 `> 总周数`，要显示"假期中"。

### 5.7 设置页弹窗范式：`MiuxSettingScreen.kt`

打开文件找"课表开始时间"那段（约 221~241 行）：

```kotlin
ArrowPreference(
    title = "课表开始时间",
    endActions = {
        Text(text = AppConfig.startDate?.toString() ?: "")   // 右侧显示当前值
    },
    onClick = {
        // 点击时设置一下初始值，然后打开弹窗
        showDialog.value = true
    }
)
```

弹窗在文件末尾（约 422~472 行）：

```kotlin
WindowDialog(
    title = "请选择开课时间",
    show = showDialog.value,
    onDismissRequest = { showDialog.value = false }   // 关闭时把 show 设回 false
) {
    Column(...) {
        // 弹窗内容：一个日期选择器
        NumberDatePicker(...)

        Row {
            TextButton(text = "取消", onClick = { dismiss?.invoke() })
            TextButton(
                text = "确认",
                onClick = {
                    AppConfig.startDate = ...   // 把选择结果写回配置
                    AppConfig.save()            // 保存
                    dismiss?.invoke()           // 关闭弹窗
                }
            )
        }
    }
}
```

**这就是你要复制的"弹窗模板"**：
1. 一个开关/箭头行（点开弹窗）。
2. 一个 `WindowDialog`，里面放控件。
3. "取消"直接关，"确认"才写回 `AppConfig` 并 `save()`。

你的滚轮（`NumberPicker`）就放进这个 `WindowDialog` 里，用两个：小时 `0..23`、分钟 `0..59`。

> Material 3 风格的设置页是另一份文件（`Material3SettingScreen.kt`），套路一样，用的是 `AlertDialog`。两处都要改。

### 5.8 本章自测
1. `buildWeekCourses(courseList)` 返回的列表，下标 0 是什么？
2. `todayCourses` 是怎么算出来的？
3. `viewModel.currentWeek` 为什么不能直接用来算"明天"？
4. 设置弹窗的"确认"按钮做了哪三件事？

---

## 第 6 章 · 把导读和你的任务连起来

学完对应章节后，就可以动手做 `TODO.md` 里对应的步骤：

| 你学完 | 可以开始做 TODO 的 |
|---|---|
| 第 1、2 章 | 能看懂所有 UI 代码，可以改设置页 |
| 第 4.2 节 + 5.3 | TODO 第一节（`AppConfig` 加字段） |
| 第 2 章 + 5.7 | TODO 第二、三节（设置页弹窗 + 滚轮） |
| 第 3 章 + 5.5 + 5.6 | TODO 第四、五节（判断逻辑 + 改 TodayScreen） |
| 第 2.3 节 + 第 3 章 | TODO 第六节（自动刷新） |

**建议动手顺序**：先在 `AppConfig` 加字段并让它能存能读 → 再做设置界面 → 最后改 `TodayScreen`。每完成一步就运行一次，别攒到最后。

### 关于滚轮组件
项目用的 Miuix 库自带 `NumberPicker`，你不需要自己写滚轮。官方"时+分"写法就是两个 `NumberPicker`：

```kotlin
Row {
    NumberPicker(value = hour,   onValueChange = {...}, range = 0..23,
                 label = { it.toString().padStart(2,'0') }, wrapAround = true)
    Text(":")
    NumberPicker(value = minute, onValueChange = {...}, range = 0..59,
                 label = { it.toString().padStart(2,'0') }, wrapAround = true)
}
```

---

## 第 7 章 · 术语小词典

| 术语 | 人话解释 |
|---|---|
| Kotlin | 一种跑在 JVM/iOS/原生上的语言，写法比 Java 简洁 |
| Compose | JetBrains 的声明式 UI 框架，用函数画界面 |
| `@Composable` | 标记"这是一个 UI 函数" |
| 重组（Recomposition） | 状态变化后，Compose 自动重新执行 UI 函数刷新界面 |
| 状态（State） | 能被 Compose 感知变化的数据，改了会触发重组 |
| `remember` | 让值在重组之间被记住，不重置 |
| `LaunchedEffect` | 进入页面后启动一段副作用（可带延时/循环） |
| 协程（Coroutine） | 可以暂停恢复的轻量线程，`delay` 不卡界面 |
| `suspend` | 标记可以暂停的函数，只能在协程里调用 |
| KMP | Kotlin Multiplatform，一份代码多平台用 |
| `expect`/`actual` | KMP 里"通用声明 + 各平台实现"的机制 |
| MVVM / ViewModel | 把数据/逻辑和界面分开的架构模式 |
| CompositionLocal | Compose 的"全局注入"，如 `LocalAppViewModel` |
| 序列化（Serializable） | 把对象转成能存储/传输的格式（这里是 JSON） |
| KVault | 加密的键值存储库（Android=加密 SharedPreferences，iOS=Keychain） |
| Lambda | 可以当参数传的一小段函数，`{ it -> ... }` |
| Elvis 运算符 `?:` | 左边为 null 就用右边的值 |
| 单例（object） | 全局只有一个实例的类 |

---

## 附：给你的三句心里话
1. **看不懂是正常的**。这份项目用了一堆工程化写法，没人第一次就能全懂。
2. **允许黑盒**。只要知道一个函数"输入什么、输出什么"，不耽误你用。
3. **小步验证**。改一小块就跑一次，报错了把错误信息发出来问，比闷头读一小时有用。

祝你早日写完这个功能。卡住随时把那几行代码发我。
